function addNumbers(x: number, y: number) {
    return x + y;
  }
  console.log(addNumbers(3, 6));

  let a: number;
  let b: string;
  let c=110;

   var recursos:string[]=['memoria','disco','procesador'];

   document.write(recursos[0].toString());
   alert(recursos[1]);